class Solution
{
    public:
    int xorOperation(int n, int start) {
        int res = start;
        while(n > 1){
            start += 2;
            res = res ^ start; // compute XOR of res and start
            n--;
        }
        return res;
    }
};
